// Auth configuration — placeholder for NextAuth v4 compatibility.
// Primary authentication is handled via server actions in app/auth/actions.ts.

export const authConfig = {
  pages: {
    signIn: "/",
  },
  providers: [],
};
